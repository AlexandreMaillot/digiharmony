# Revue de conformité aux règles — Phase 1 (branche `alexandre`, `git diff main`)

> Date : 2026-06-05 · Périmètre : `apps/digiharmony_app/lib/**` + `test/**` du diff (générés exclus : `*.g.dart`, `lib/l10n/gen/**`).
> Source des règles : `.claude/rules/**/*.mdc` (priorité `alwaysApply: true` + globs Dart) + memory bank (`coding-assertions`, `architecture`, `design-system`, DEC-001/002/003) + `CLAUDE.md`.
> Posture : RAPPORT uniquement. Aucune correction appliquée.

---

## 0. Note de cadrage — contradiction règle ↔ memory bank

Le fichier `aidd_docs/memory/architecture.md` (lignes 39, 42, 51, 54, 68, 73, 80) **et** `CLAUDE.md` nomment explicitement `LocaleCubit` / `BienvenueCubit` et le couple « Bloc / Cubit ». La règle `01-standards/1-bloc-only-no-cubit.mdc` (`alwaysApply: true`, globs `apps/*/lib/**/*.dart`) **interdit tout Cubit, sans exception**.

Les deux sources se contredisent. Le validateur de cette revue étant le dossier `.claude/rules/`, je score **contre la règle** (Cubit = violation), mais je signale la contradiction : elle doit être tranchée par le caller (soit la règle prime → convertir les Cubits, soit le memory bank prime → assouplir/abroger la règle). Tant qu'elle n'est pas tranchée, le code reste non conforme à une règle `alwaysApply: true`.

---

## 1. Revue règle par règle

### 1.1 `01-standards/1-bloc-only-no-cubit.mdc` — `alwaysApply: true`
**Statut : VIOLÉ — critical.**

`Cubit` interdit. Occurrences `extends HydratedCubit` :
- `lib/locale/locale_cubit.dart:9` — `class LocaleCubit extends HydratedCubit<Locale?>`
- `lib/bienvenue/bienvenue_cubit.dart:7` — `class BienvenueCubit extends HydratedCubit<bool>`

De plus, ces Cubits exposent des mutations directes hors du flux event→state, ce que la règle prohibe aussi (« `emit(...)` direct dans la view sans `add(Event)` ») :
- `locale_cubit.dart:14` `setLocale` → `emit(locale)`, `:17` `useSystem` → `emit(null)`
- `bienvenue_cubit.dart:17` `complete` → `emit(true)`

Correctif décrit (non appliqué) :
- Convertir `LocaleCubit` → `LangueBloc extends HydratedBloc<LangueEvenement, LangueEtat>` (events `LangueDefinie(locale)`, `LangueSystemeDemandee`).
- Convertir `BienvenueCubit` → `BienvenueBloc extends HydratedBloc<BienvenueEvenement, BienvenueEtat>` (event `BienvenueMarqueeVue`, état exposant `estVue`).
- Répercuter dans `app/view/app.dart` (BlocProvider), `demarrage_bloc.dart` (dépendance injectée), et tous les tests `*_cubit_test.dart`.
- OU : faire trancher la contradiction §0 par le caller avant tout refactor.

---

### 1.2 Nommage FR des Bloc (events `…Evenement`, states `…Etat`) — couvert par `1-bloc-only-no-cubit.mdc` (l.18-20) + `1-french-naming-code.mdc` (`alwaysApply: true`)
**Statut : VIOLÉ — high.**

La règle impose suffixes FR `Evenement` / `Etat`. Le code utilise les suffixes anglais `Event` / `State` :
- `lib/accueil/bloc/accueil_event.dart:4` `sealed class AccueilEvent` → attendu `AccueilEvenement`
- `lib/accueil/bloc/accueil_state.dart:52` `sealed class AccueilState` → attendu `AccueilEtat`
- `lib/demarrage/bloc/demarrage_event.dart:5` `sealed class DemarrageEvent` → attendu `DemarrageEvenement`
- `lib/demarrage/bloc/demarrage_state.dart:5` `sealed class DemarrageState` → attendu `DemarrageEtat`

Nuance : `1-french-naming-code.mdc:38` tolère `Event`/`State` comme suffixes techniques. Mais `1-bloc-only-no-cubit.mdc:18-20` est plus strict et explicite (`Evenement`/`Etat`). En cas de conflit entre deux règles `alwaysApply: true`, la plus spécifique (Bloc) prime → violation maintenue.

Correctif décrit : renommer les 4 classes de base + leurs sous-classes ne change pas (déjà FR : `AccueilDemarre`, `DemarrageDemarre`, `AccueilPret`, etc.), renommer fichiers `*_event.dart`/`*_state.dart` → `*_evenement.dart`/`*_etat.dart` et mettre à jour les `part`/`part of`.

---

### 1.3 `01-standards/1-flutter-page-methods.mdc` — `alwaysApply: true` (globs `apps/*/lib/pages/**/*_page.dart`)
**Statut : VIOLÉ — high (avec réserve de glob, voir 1.4).**

La règle exige sur chaque page `static MaterialPage<void> page()` + `static Route<void> route()` + un `BlocProvider(create: ... ..add(Event))`.

Recherche `static MaterialPage` / `static Route` dans `lib/` : **0 occurrence**. Pages concernées :
- `lib/accueil/accueil_page.dart` — expose le `BlocProvider..add(AccueilDemarre())` (OK partiel, l.18-24) mais **aucune** méthode `page()`/`route()`.
- `lib/demarrage/view/demarrage_page.dart` — `BlocProvider` présent (l.19-26) **sans `..add()`** (l'event est envoyé depuis `didChangeDependencies` de la View, `demarrage_view.dart:42`) ; **aucune** `page()`/`route()`.
- `lib/bienvenue/bienvenue_page.dart` — simple `StatelessWidget` Scaffold, pas de Bloc, pas de `page()`/`route()`.

Réserve : le glob de la règle vise `lib/pages/**` ; le projet n'a pas de dossier `pages/` (voir 1.4), donc l'application littérale du glob est nulle. Je classe la règle VIOLÉE car son intention (centralisation navigation via `page()`/`route()`) n'est satisfaite nulle part, et `app_router.dart` réimplémente des `MaterialPageRoute` inline à la place.

Correctif décrit : ajouter `static MaterialPage<void> page()` + `static Route<void> route()` aux 3 pages ; déplacer le `..add(DemarrageDemarre(...))` dans le `create` du `BlocProvider` de `demarrage_page.dart` (la durée minimale devra être résolue autrement que via `MediaQuery` du `didChangeDependencies`, p. ex. event paramétré au `create` ou lecture dans la View conservée mais documentée). Faire consommer `route()`/`page()` par `app_router.dart`.

---

### 1.4 `00-architecture/0-flutter-pages-structure.mdc` — `alwaysApply: false` (globs `apps/*/lib/pages/**`)
**Statut : VIOLÉ — medium.**

Structure imposée : `lib/pages/<page>/bloc/<page>_bloc/` + `views/`. Réalité (`find lib -type d`) :
- Pas de dossier `lib/pages/`. Les features sont à la racine de `lib/` : `lib/accueil/`, `lib/demarrage/`, `lib/bienvenue/`.
- `accueil` : `bloc/` (sans sous-dossier `accueil_bloc/`), `view/`, `widgets/`, et la page est à `lib/accueil/accueil_page.dart` (hors `view/`).
- `demarrage` : `bloc/` (sans `demarrage_bloc/`), `view/` (contient page + view), `widgets/`.

Écarts : (a) absence de `pages/`, (b) absence du niveau `<page>_bloc/` sous `bloc/`, (c) dossier nommé `view/` (singulier) au lieu de `views/`, (d) `accueil_page.dart` hors du dossier de vues.

Correctif décrit : soit migrer vers `lib/pages/accueil/{bloc/accueil_bloc/, views/}` etc. conformément à la règle, soit (si l'équipe assume une convention « feature-folder » distincte) acter une dérogation explicite dans le memory bank + ajuster les globs des règles concernées. En l'état, c'est une non-conformité réelle à arbitrer.

---

### 1.5 `03-frameworks-and-libraries/3-flutter-view-structure.mdc` — `alwaysApply: true` (globs `*_view.dart`)
**Statut : VIOLÉ — medium.**

Exigences : `Scaffold` (OK), `BlocBuilder` (OK accueil), états chargement/erreur/vide/succès **via extensions enum status** (`etat.status.estEnChargement`…), widgets `StatelessWidget` séparés (pas de `_buildX`).

Écarts :
- **Pas de pattern enum `status` + extension** : les states sont des `sealed class` discriminées par `is` (`accueil_view.dart:56-67` `state is AccueilChargement` / `state is AccueilPret`). La règle (et `1-dart-enum-extensions.mdc`, `3-flutter-bloc-events-states.mdc`) attend un `enum …Status` + extension `est…`. Voir 1.7.
- **État vide absent** : seuls chargement/pret(succès)/erreur existent ; pas d'état « vide » distinct (acceptable fonctionnellement — l'« absence d'humeur » est l'état A, pas un état vide d'écran — mais s'écarte du gabarit).
- `demarrage_view.dart` : c'est un `StatefulWidget` avec `BlocListener` (pas `BlocBuilder`) et **méthodes `_blocMarque`/`_logo`/`_footer` retournant des Widgets** (l.81, 133, 164) → voir 1.6.

Correctif décrit : aligner les states sur le pattern `status` (1.7), extraire les `_build*` de `demarrage_view` (1.6). L'usage d'un `BlocListener` pour le routage du splash est défendable, mais à documenter comme dérogation au gabarit `BlocBuilder`.

---

### 1.6 `01-standards/01-widget-extraction.mdc` + `03-frameworks-and-libraries/3-flutter-stateless-widgets.mdc` — `alwaysApply: true`
**Statut : VIOLÉ — high.**

Interdiction de toute méthode privée retournant un `Widget`. Occurrences :
- `lib/demarrage/view/demarrage_view.dart:81` `Widget _blocMarque(...)`
- `lib/demarrage/view/demarrage_view.dart:133` `Widget _logo(...)`
- `lib/demarrage/view/demarrage_view.dart:164` `Widget _footer()`
- `lib/demarrage/widgets/points_chargement.dart:30` `Widget _point(...)`
- `lib/demarrage/widgets/anneaux_ondes.dart:29` `Widget _anneau(...)`

Note : `halo_respirant`/`particules_flottantes`/`pilule_action`/`tuile_outil` construisent leur widget dans le `build()` (variable locale `final pilule = …`), ce qui est conforme (pas de méthode helper). `accueil_view.dart` extrait correctement `_Header`, `_Greeting`, `_SkeletonHeroCard` en `StatelessWidget` privés → conforme.

Correctif décrit : extraire `_blocMarque`, `_logo`, `_footer` en `StatelessWidget` (`_BlocMarque`, `_LogoAnime`, `_FooterFinancement`) ; extraire `_point` → `_PointChargement`, `_anneau` → `_AnneauOnde`.

---

### 1.7 `03-frameworks-and-libraries/3-flutter-bloc-events-states.mdc` + `01-standards/1-dart-enum-extensions.mdc` — `alwaysApply: true` (globs `*_state.dart`)
**Statut : VIOLÉ — medium.**

La règle states impose : `final class …Etat extends Equatable` + `copyWith` + `enum …Status` + extension `…StatusX` (getters `estEnChargement`…). Le code :
- N'utilise **pas `Equatable`** : `accueil_state.dart`, `demarrage_state.dart` réimplémentent `==`/`hashCode` à la main (ex. `accueil_state.dart:60-64`, `demarrage_state.dart:14-18`). Fonctionnellement correct mais hors gabarit (duplication évitable, risque d'oubli de champ).
- N'utilise **pas** d'`enum …Status` ni d'extension `est…` : modèle « sealed class par état » à la place. Donc `1-dart-enum-extensions.mdc` (qui s'applique aux `*_state.dart`) est sans objet matériel → non respecté dans l'esprit.
- Pas de `copyWith` (cohérent avec des états immuables sans champ mutable, mais s'écarte du gabarit).

Correctif décrit : soit migrer vers le pattern `enum AccueilStatus { chargement, pret, erreur }` + `extension AccueilStatusX` + `final class AccueilEtat extends Equatable` avec `copyWith`, soit acter formellement la convention « états sealed sans Equatable » comme dérogation projet (cohérente, mais à documenter pour lever l'ambiguïté de la règle).

---

### 1.8 `03-frameworks-and-libraries/3-flutter-bloc-concurrency.mdc` — `alwaysApply: true` (globs `*_bloc.dart`)
**Statut : VIOLÉ — high.**

La règle impose un `transformer:` **explicite** sur chaque `on<Event>` + import `bloc_concurrency`.
- `grep transformer` sur `lib/accueil/bloc` + `lib/demarrage/bloc` : **0 occurrence**.
- `grep bloc_concurrency` sur `lib/` + `pubspec.yaml` : **0 occurrence** (dépendance absente).
- `accueil_bloc.dart:31` `on<AccueilDemarre>(_onDemarre);` — sans transformer. Le doc-comment (l.39) prétend « Restartable » mais aucun `restartable()` n'est branché → comportement par défaut `concurrent`, contradiction code/commentaire.
- `demarrage_bloc.dart:30` `on<DemarrageDemarre>(_onDemarre);` — sans transformer.

Correctif décrit : ajouter `bloc_concurrency` au `pubspec.yaml` ; brancher `transformer: restartable()` sur `AccueilDemarre` (cohérent avec le ré-abonnement Drift annoncé) et `transformer: restartable()` (ou `droppable()`) sur `DemarrageDemarre` ; corriger le commentaire « Restartable » de `accueil_bloc.dart`.

---

### 1.9 `07-quality-assurance/7-catch-silencieux-lookup.mdc` — `alwaysApply: false` (globs `apps/**/lib/**/*.dart`)
**Statut : VIOLÉ — high.**

Catch silencieux interdit sur chemin critique (doit logger `dart:developer` + feedback UX).
- `lib/accueil/bloc/accueil_bloc.dart:63` `} on Object {` puis `emit(const AccueilErreur());` — **aucun `developer.log`**, aucun `error`/`stack` capturé. Le commentaire reconnaît un « fallback silencieux ». Le `onError` du `forEach` (l.61) `(error, stack) => const AccueilErreur()` **ignore aussi** `error`/`stack` sans log.
- `lib/demarrage/bloc/demarrage_bloc.dart:50` `} on Object {` → re-délai puis `emit(DemarrageErreur(...))` — **aucun log** de l'exception du warm-up Drift.

Ces deux blocs sont sur le chemin critique de démarrage/affichage (lecture Drift). Un échec d'ouverture de base resterait invisible en debug, exactement le scénario que la règle (PR #808) vise à interdire.

Nuance : le fallback UX existe (états `AccueilErreur`/`DemarrageErreur` rendent un écran dégradé) → l'exigence « feedback UX » est partiellement satisfaite. C'est le **log `dart:developer`** qui manque systématiquement, capturant `error`/`stack`.

Correctif décrit : dans les deux `catch` et dans le `onError` du `forEach`, ajouter `developer.log('<contexte>', error: e, stackTrace: stack is StackTrace ? stack : null, name: 'accueil'|'demarrage')` avant l'`emit`. Capturer `e, stack` (`} on Object catch (e, stack) {`).

---

### 1.10 `03-frameworks-and-libraries/3-flutter-i18n.mdc` — `alwaysApply: true` (globs `*_view.dart`, `*_page.dart`)
**Statut : VIOLÉ — medium.**

« INTERDIRE tout texte en dur, aucune exception, même pour le debug. »
- `lib/demarrage/view/demarrage_view.dart:83` `'DIGIHARMONY'` en dur dans un `Text`.

Le wordmark de l'Accueil, lui, passe par `l10n.homeBrandName.toUpperCase()` (`accueil_view.dart:204`) — conforme et incohérent avec le splash. La règle ne prévoit pas d'exception « marque » ; même si DEC-HOME-01 dit « wordmark non traduit », il existe déjà une clé i18n (`homeBrandName`) qui retourne le même texte non traduit dans toutes les langues.

Correctif décrit : remplacer le littéral `'DIGIHARMONY'` de `demarrage_view.dart:83` par `l10n.homeBrandName.toUpperCase()` (ou une clé dédiée `splashBrandName`), pour aligner sur l'Accueil et respecter la règle.

---

### 1.11 `03-frameworks-and-libraries/3-hydrated-bloc.mdc` — `alwaysApply: true` (globs `main_*.dart`)
**Statut : PARTIEL — medium.**

La règle exige l'init `HydratedBloc.storage` dans `main_*.dart` avec **split web/mobile** (`HydratedStorageDirectory.web` vs `getApplicationDocumentsDirectory()`), précédé de `WidgetsFlutterBinding.ensureInitialized()`.

Réalité :
- L'init n'est **pas** dans `main_*.dart` mais centralisée dans `bootstrap.dart:57` (`HydratedBloc.storage = await (storageBuilder ?? _defaultStorageBuilder)()`), appelée par les 3 `main_*.dart`. `ensureInitialized()` présent (`bootstrap.dart:48`). Ordre correct (storage avant `runApp`).
- **Aucun split web/mobile** : `_defaultStorageBuilder` (`bootstrap.dart:26-31`) utilise toujours `getApplicationDocumentsDirectory()`, sans branche `kIsWeb` → `HydratedStorageDirectory.web`. Pour une app Android-only c'est sans impact runtime, mais la règle `alwaysApply: true` demande explicitement le split.

Correctif décrit : si le web n'est jamais une cible, acter une dérogation (app mobile-only) ; sinon, ajouter la branche `kIsWeb` dans `_defaultStorageBuilder`. À défaut, conforme « mobile » mais non conforme à la lettre de la règle.

---

### 1.12 `03-frameworks-and-libraries/3-flutter-error-handler-web.mdc` — `alwaysApply: true` (globs `bootstrap.dart`, `main*.dart`)
**Statut : VIOLÉ — low (medium si web visé).**

`FlutterError.onError` doit filtrer `stack` (`stackTrace: stack is StackTrace ? stack : null`) pour éviter le crash `LegacyJavaScriptObject` en Flutter web dev (DDC).
- `lib/bootstrap.dart:50-52` : `FlutterError.onError = (details) { log(details.exceptionAsString(), stackTrace: details.stack); };` → passe `details.stack` brut, **sans le garde `is StackTrace`**. C'est exactement l'anti-pattern interdit (`3-flutter-error-handler-web.mdc:23-26`).
- Pas de `PlatformDispatcher.instance.onError` défini (la règle le mentionne mais ne l'impose pas).

Sévérité low car cible actuelle Android (bug DDC web-only) ; medium si le web devient cible.

Correctif décrit : `final stack = details.stack; log(details.exceptionAsString(), stackTrace: stack is StackTrace ? stack : null);`.

---

### 1.13 `01-standards/1-clean-code.mdc` — `alwaysApply: true`
**Statut : PARTIEL — low.**

- **300 lignes/fichier** : OK (max `accueil_view.dart` = 291 < 300 ; tous les autres en dessous). Tests : `accueil_bloc_test.dart` = 214 < 300, OK.
- **30 lignes/fonction** : OK sur l'échantillon (build de `accueil_view` ~125 lignes mais c'est une méthode `build()` — tolérée par `01-widget-extraction` exceptions ; néanmoins long, candidate à extraction supplémentaire).
- **« Ne pas écrire de commentaires »** : largement enfreint — commentaires explicatifs nombreux (`accueil_view.dart:26,37,47…`, doc-comments partout). À noter que cette directive « zéro commentaire » entre en tension avec les doc-comments `///` exigés par le lint `very_good_analysis` (`public_member_api_docs`). Tension de règles → je classe PARTIEL/low, non bloquant, à arbitrer (la directive « pas de commentaires » vise vraisemblablement les commentaires de bruit, pas la doc d'API).
- **10 sous-fichiers/dossier** : OK.

Correctif décrit : aucun changement structurel requis ; arbitrer la directive « zéro commentaire » vs `public_member_api_docs`.

---

### 1.14 `01-standards/1-french-naming-code.mdc` — `alwaysApply: true`
**Statut : PARTIEL — low.**

- Classes/dossiers/fichiers majoritairement FR (`AccueilBloc`, `DemarrageView`, `CarteHumeur`, `PiluleAction`, `HaloRespirant`, `ouvrirPlaceholder`…) → bon.
- Scaffolding EN toléré explicitement par le memory bank (`AppTheme`, `AppColors`, `AppDatabase`, `AppRouter`, `LocaleCubit`, `bootstrap`) → conforme à la dérogation actée (architecture.md:68).
- Écarts mineurs : variables anglaises résiduelles dans le corps — `final locale = …` (`app/view/app.dart:43`, `carte_humeur.dart:109`), `var _demarre`/`reduit` (mixte). La règle préfère `langue`/`locale` est un terme Flutter toléré. Codes d'émotion EN (`happy`, `calm`…) = valeurs de code stables (DEC-003), tolérées.
- Suffixes `Event`/`State` EN → déjà compté en 1.2 (ne pas double-compter).

Correctif décrit : renommer les variables locales EN résiduelles ; sans impact fonctionnel.

---

### 1.15 Règles thème — `3-flutter-colorscheme`, `3-flutter-texttheme-current`, `3-flutter-withvalues`, `3-flutter-theme-buttons` — `alwaysApply: true`
**Statut : CONFORME (avec 1 réserve mineure).**

- `withValues(alpha:)` partout, **aucun `withOpacity`** (grep = 0). Conforme `3-flutter-withvalues`.
- Aucune propriété `ColorScheme` dépréciée (`surfaceVariant`/`background`/`onBackground`) — les hits `AppColors.background` sont des constantes locales, pas des accès `colorScheme`. Conforme `3-flutter-colorscheme`.
- Aucune propriété `textTheme` dépréciée (`headline1`, `bodyText1`, `caption`…). Conforme `3-flutter-texttheme-current`. Réserve : la « convention responsive » (Small=mobile, etc.) n'est pas suivie (usage de `headlineMedium`, `bodyLarge`, `titleLarge` sans fichiers `_view_mobile/_tablette/_bureau`) — mais le projet n'a pas de découpage responsive par fichier, donc la sous-règle est sans objet matériel → toléré.
- `3-flutter-theme-buttons` : `theme.dart:134` `minimumSize: const Size(48, 48)` — **pas** de `Size(double.infinity, …)`. Conforme (l'interdiction porte sur `double.infinity` en width). Les boutons full-width sont gérés au site d'appel via `SizedBox(width: double.infinity)` (`carte_humeur.dart:75`) → pattern idiomatique recommandé. Conforme.

---

### 1.16 `02-programming-languages/2-dart-enum-byname.mdc` — `alwaysApply: true` (globs `**/*.dart`)
**Statut : VIOLÉ — medium.**

« INTERDIRE les switch/Map pour convertir String → Enum ; utiliser `Enum.values.byName()`. » Ici la conversion est String → texte/couleur (pas vers un enum), mais l'esprit (pas de mapping manuel dupliqué) est enfreint par des `switch` String répétés et un `Map` :
- `lib/accueil/view/accueil_view.dart:148-166` `_resoudreConseil` : `switch` de 7 cas `'tipDay01'…'tipDay07'`.
- `lib/accueil/widgets/carte_humeur.dart:187-205` `_libelleEmotion` : `switch` de 7 cas `'happy'…'tired'`.
- `lib/accueil/bloc/accueil_bloc.dart:9-17` `emojiParCode` : `Map<String,String>` manuel.
- `lib/theme/theme.dart:63-71` `MoodColors.byKey` : `Map<String,Color>` manuel.

L'absence d'un `enum Emotion { happy, calm, … }` côté app force ces mappings String manuels (le code commente « aligné sur le futur enum métier `core_package` »). La règle `2-dart-enum-byname` reste techniquement non applicable faute d'enum, mais le **design** contourne l'intention (pas de source unique typée). Les `switch` i18n (`l10n.tipDayNN`/`l10n.moodXxx`) sont en partie incontournables (getters générés statiques), mais le mapping code→couleur/emoji pourrait être unifié.

Correctif décrit : à terme, introduire `enum Emotion` (clé canonique) avec extension portant `couleur`, `emoji`, `cleI18n`, et parser via `Emotion.values.byName(code)` (try/catch → défaut). Réduit `emojiParCode` + `MoodColors.byKey` + `_libelleEmotion` à une seule source. Non bloquant pour la Phase 1 mais dette à acter.

---

### 1.17 `03-frameworks-and-libraries/3-flutter-gen-l10n-avant-test.mdc` — `alwaysApply: false`
**Statut : N/A (process, pas code).** Règle de workflow (lancer `flutter gen-l10n` avant `flutter test`). Les 8 ARB ont été modifiés dans le diff ; la conformité est procédurale (CI/local), non vérifiable sur l'artefact statique. Aucune violation de code. À rappeler au caller pour l'exécution des tests.

---

### 1.18 Règles hors périmètre — N/A
Toutes les règles Firebase / Firestore / Dart-Frog / `athena_*` / React / Tailwind / MobX / Python / PHP / TypeScript / admin / pricing / notifications / worktree / stacked-PR : **N/A** (globs ne matchant pas `digiharmony_app`, ou stack absente — projet sans backend/Firebase par conception, CLAUDE.md). `2-dart-naming-conventions` (PascalCase classes, camelCase méthodes/fichiers) : CONFORME. `3-flutter-listview-separated`, `3-flutter-mediaquery-extension`, `3-flutter-visibility-widget`, `3-flutter-interactive-widgets`, `3-flutter-chip-*`, `3-flutter-dialog-*`, `3-flutter-textfield-*`, `3-flutter-dropdown-*`, `3-flutter-date-picker`, `3-flutter-formz-*` : N/A (widgets/composants non utilisés en Phase 1).

---

## 2. Récapitulatif

### Répartition par sévérité (10 violations + 3 partiels)

| Sévérité | Nb | Règles |
|---|---|---|
| critical | 1 | 1.1 bloc-only-no-cubit |
| high | 5 | 1.2 nommage Evenement/Etat · 1.3 page()/route() · 1.6 widget-extraction · 1.8 bloc-concurrency/transformer · 1.9 catch silencieux |
| medium | 4 | 1.4 pages-structure · 1.5 view-structure · 1.7 events-states (Equatable/status) · 1.10 i18n DIGIHARMONY · 1.16 enum-byname |
| low | 2 | 1.12 error-handler-web · (1.13/1.14 partiels) |
| partiel | 3 | 1.11 hydrated-bloc (split web) · 1.13 clean-code · 1.14 french-naming |

> Note : 1.10 et 1.16 listés en medium (1.10 compté une fois). Total entrées VIOLÉ = 10 ; PARTIEL = 3 ; CONFORME = 1.15, 2-dart-naming ; N/A = §1.17-1.18.

### Hard blockers
- **1.1 (critical)** : violation d'une règle `alwaysApply: true` à exception « Aucune ». Bloque la conformité tant que la contradiction §0 n'est pas tranchée par le caller.

---

## 3. Plan de remédiation ordonné (TOP 5 refactors)

1. **Trancher §0 puis convertir les Cubits → Bloc** (1.1, critical). Décision caller d'abord (règle vs memory bank). Si la règle prime : `LocaleCubit`→`LangueBloc`, `BienvenueCubit`→`BienvenueBloc` (HydratedBloc), répercuter `app.dart`, `demarrage_bloc.dart`, tests. Impact transverse → à faire en premier car il conditionne le nommage (1.2) et la structure des dossiers.
2. **Brancher `bloc_concurrency` + transformers explicites** (1.8, high). Ajouter la dépendance, `restartable()` sur `AccueilDemarre` et `DemarrageDemarre`, corriger le commentaire trompeur d'`accueil_bloc.dart:39`. Petit diff, supprime une contradiction code/doc.
3. **Logger les catch silencieux du chemin critique** (1.9, high). `developer.log(error:, stackTrace: stack is StackTrace ? stack : null)` dans `accueil_bloc.dart:61/63` et `demarrage_bloc.dart:50` ; + corriger `FlutterError.onError` (1.12). Faible coût, supprime une classe de bugs invisibles.
4. **Extraire les méthodes `_buildX` en StatelessWidget** (1.6, high) et harmoniser le nommage `Evenement`/`Etat` + fichiers (1.2, high). Refactor mécanique sur `demarrage_view.dart`, `points_chargement.dart`, `anneaux_ondes.dart` et les 4 fichiers event/state.
5. **Aligner structure & gabarit page/state** (1.3, 1.4, 1.5, 1.7, medium). Plus lourd : soit migrer vers `lib/pages/<page>/{bloc/<page>_bloc/, views/}` + `page()`/`route()` + pattern `enum status`+`Equatable`+`copyWith`, soit acter formellement les dérogations « feature-folder » et « états sealed sans Equatable » dans le memory bank et ajuster les globs des règles. À décider globalement pour éviter un refactor à moitié fait.

Hors top 5 (dette à planifier) : 1.16 (enum `Emotion` unifié), 1.10 (i18n du wordmark splash), 1.11 (split web HydratedBloc si web visé).
