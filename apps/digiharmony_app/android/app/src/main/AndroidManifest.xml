<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Plan : seule permission declaree (Mon temps d'ecran, best-effort).
         Permission protegee -> octroyee via l'ecran systeme ACTION_USAGE_ACCESS_SETTINGS. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- flutter_local_notifications : rappel quotidien humeur (100 % local,
         zéro réseau). Requise sur Android 13+ (API 33+) pour afficher des
         notifications. Jamais de VIBRATE ni RECEIVE_BOOT_COMPLETED (DEC-R-04). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Alarmes exactes : rappel quotidien pile à l'heure choisie (DEC-010,
         permission fonctionnelle zéro collecte). Sur Android 12+, l'utilisateur
         doit activer « Alarmes et rappels » dans les réglages système (demandé
         à l'activation du rappel). Repli automatique en mode inexact si non
         accordée. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- just_audio_background : lecture continue de l'ambiance Detox (asset
         local, AUCUN reseau). Service media en avant-plan. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <application
        android:label="${appName}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- just_audio_background service + receiver (media lockscreen/notif). -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
        <!-- flutter_local_notifications : receiver QUI POSTE la notification
             planifiée quand l'alarme exacte se déclenche (rappel quotidien
             d'humeur). OBLIGATOIRE — le plugin ne le déclare pas lui-même ;
             sans lui, l'alarme tire mais aucune notif n'est postée.
             exported=false : déclenché uniquement par le PendingIntent interne.
             PAS de ScheduledNotificationBootReceiver (RECEIVE_BOOT_COMPLETED
             interdit, DEC-R-04) : le rappel est replanifié au lancement de
             l'app, pas besoin de survivre au reboot. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher (Android 11+) : ouvrir des liens web https/http
             (Paramètres : GitHub) — sans ces <intent>, canLaunchUrl renvoie
             false (« component name is null »). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <!-- url_launcher : composer un numero tel: (Soutien : ligne d'ecoute). -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
